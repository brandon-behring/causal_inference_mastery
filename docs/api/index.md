# API Reference

Complete Python API reference for causal-inference-mastery, organized by method family.

## Package Structure

| Category | Subpackages |
|----------|-------------|
| **Experiments** | {doc}`rct` |
| **Selection on Observables** | {doc}`observational`, {doc}`psm` |
| **Natural Experiments** | {doc}`did`, {doc}`rdd`, {doc}`rkd`, {doc}`scm` |
| **Endogeneity** | {doc}`iv`, {doc}`control-function`, {doc}`shift-share` |
| **Heterogeneous Effects** | {doc}`cate`, {doc}`qte`, {doc}`mte` |
| **Sensitivity & Bounds** | {doc}`sensitivity`, {doc}`bounds` |
| **Advanced** | {doc}`mediation`, {doc}`bunching`, {doc}`principal-strat` |
| **Time Series** | {doc}`timeseries` |
| **Discovery** | {doc}`discovery` |
| **Dynamic** | {doc}`dtr`, {doc}`panel` |
| **Bayesian** | {doc}`bayesian` |
| **Utilities** | {doc}`utils` |
